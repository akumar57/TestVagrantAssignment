1. all global level parameters like URL, API_Endpoint, variance value can be customized from config.properties defined in src/main/java source folder. 

2 .We just need to update the path for config.properties file after downloading in local system

3. Just execute 1 test case "TemperatureFromNDTV" which will perform all validations and will print the results in console.